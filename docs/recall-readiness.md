# Recall readiness

Feature code and the isolated database schema are implemented on `feature/adaptive-recall-quiz`.

Remaining release gates are a production build / preview-deployment pass and an authenticated browser smoke test before merging to `main`.
